name: Test SipShop Monitor

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test GitHub Actions
        run: |
          echo "================================"
          echo "SipShop Story Monitor"
          echo "GitHub Actions BERHASIL!"
          echo "================================"
          date
